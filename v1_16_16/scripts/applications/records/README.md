# Helper-managed application records

This directory contains Admin build-source metadata created by the v1.16.16 application helpers.
It is **not** copied into the DevContainer image and is **not** a runtime application catalog.
